# Agent Guidelines

- Use `uv` for Python virtual environment creation and package management.
  - Virtual environment creation: `uv venv`
  - Package installation: `uv pip install ...` or `uv add ...`
  - Running commands: `uv run ...`
